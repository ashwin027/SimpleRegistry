apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-app
  namespace: simpleregistry
  labels:
    app: user-app
spec:
  selector:
    matchLabels:
      app: user-app
  template:
    metadata:
      labels:
        app: user-app
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "userapp"
        dapr.io/app-port: "80"
        dapr.io/log-as-json: "true"
    spec:
      containers:
      - name: user-app
        image: ashwin027/userapi:latest
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: userapp-dapr-ingress
  namespace: simpleregistry
spec:
  rules:
  - host: user-app.internal
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: userapp-dapr
            port:
              number: 80